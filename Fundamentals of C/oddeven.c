//Program to compute whether number is odd or even
#include <stdio.h>

main() {
	int n;
	printf ("Enter the number: ");
	scanf ("%f",&n);
	if (n%2 == 0)
	    printf ("The number is even");
    else
        printf ("The number is even");
}
