//Program to find square root of a number
#include <stdio.h>
#include <math.h>

main() {

	float n,s;
	printf ("Enter the number: ");
	scanf ("%f",&n);
	s = sqrt(n);
	printf("The square root is %f",s);
}
